{
  "name": "rushanhaque-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Static site + admin panel. No bundler: the build step only validates the host config.",
  "scripts": {
    "build": "node scripts/validate-config.mjs",
    "validate": "node scripts/validate-config.mjs",
    "check:live": "node scripts/check-live.mjs",
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "engines": { "node": ">=18" }
}
